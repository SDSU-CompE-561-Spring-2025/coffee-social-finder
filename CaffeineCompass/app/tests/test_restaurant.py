import pytest
import restaurant_tag

def test_restaurant()
    pn = Restaurant(id = 1, address = "test", name= "John", rating = 5, phone_number = "6195945200" )

    assert pn.id == 1
    assert pn.address == test
    assert pn.name == "John"
    assert pn.rating == 5
    assert pn. phone_number == "6195945200"
